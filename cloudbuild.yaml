steps:
- name: node:20.0.0
  entrypoint: npm
  args: ["install"]
- name: node:20.0.0
  entrypoint: npm
  args: ["run", "build"]
build:
  service_account: [soloway.ivan.412@gmail.com]
  options:
    default_logs_bucket_behavior: REGIONAL_USER_OWNED_BUCKET
timeout: "1600s"

